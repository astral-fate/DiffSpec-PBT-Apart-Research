# Generated by DiffSpec-PBT live mode
# provider: groq
# model: groq/compound
# timestamp_utc: 2026-05-25T00:38:20Z
# extract_status: ok
# elapsed_seconds: 3
def pre(i):
    return (
        isinstance(i, tuple)
        and len(i) == 2
        and isinstance(i[0], list)
        and all(isinstance(x, int) for x in i[0])
        and i[0] == sorted(i[0])
        and isinstance(i[1], int)
    )

def post(i, o):
    lst, target = i
    if o == -1:
        return target not in lst
    else:
        return (
            isinstance(o, int)
            and 0 <= o < len(lst)
            and lst[o] == target
        )
