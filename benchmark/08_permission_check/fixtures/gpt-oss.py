# Generated by DiffSpec-PBT live mode
# provider: groq
# model: openai/gpt-oss-120b
# timestamp_utc: 2026-05-25T00:36:40Z
# extract_status: ok
# elapsed_seconds: 2
def pre(i):
    # Input must be a tuple of (user_id, allowed_list, active_flag)
    if not isinstance(i, tuple) or len(i) != 3:
        return False
    user_id, allowed_list, active_flag = i
    # active_flag must be a boolean
    if not isinstance(active_flag, bool):
        return False
    # allowed_list must be a list, tuple, or set
    if not isinstance(allowed_list, (list, tuple, set)):
        return False
    # user_id must be hashable
    try:
        hash(user_id)
    except Exception:
        return False
    # every element in allowed_list must be hashable
    for elem in allowed_list:
        try:
            hash(elem)
        except Exception:
            return False
    return True


def post(i, o):
    # Output must be a boolean
    if not isinstance(o, bool):
        return False
    user_id, allowed_list, active_flag = i
    expected = active_flag and (user_id in allowed_list)
    return o == expected
