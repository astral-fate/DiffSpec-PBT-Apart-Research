# Tokenizer Truncate

Given a list of token IDs and a non-negative integer N, return the prefix of the input containing at most N tokens. The result must be exactly the first min(N, len(tokens)) tokens, in order.
