# Generated by DiffSpec-PBT live mode
# provider: groq
# model: groq/compound
# timestamp_utc: 2026-05-25T00:38:23Z
# extract_status: ok
# elapsed_seconds: 2
def pre(i):
    # Input must be a pair (list_of_ints, k) where k is a positive integer
    if not isinstance(i, (list, tuple)) or len(i) != 2:
        return False
    lst, k = i
    if not isinstance(lst, list):
        return False
    if not all(isinstance(x, int) for x in lst):
        return False
    if not isinstance(k, int) or k <= 0:
        return False
    return True

def post(i, o):
    # Correct output for a valid input
    if not pre(i):
        return False
    lst, k = i
    n = len(lst)
    if k > n:
        return o is None
    # k-th smallest (1‑based) element
    kth = sorted(lst)[k - 1]
    return o == kth
