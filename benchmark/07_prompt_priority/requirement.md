# Prompt Priority Assembly

Given a system prompt, a user prompt, and an ordered list of retrieved sources, assemble a single context list of strings in priority order: system first, then user, then retrieved sources in their given order.
