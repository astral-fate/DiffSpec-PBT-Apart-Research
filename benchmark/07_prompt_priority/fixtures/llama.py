# Generated by DiffSpec-PBT live mode
# provider: nim
# model: meta/llama-3.3-70b-instruct
# timestamp_utc: 2026-05-24T23:56:31Z
# extract_status: ok
# elapsed_seconds: 4
def pre(i):
    return isinstance(i, tuple) and len(i) == 3 and isinstance(i[0], str) and isinstance(i[1], str) and isinstance(i[2], list) and all(isinstance(x, str) for x in i[2])

def post(i, o):
    if not pre(i):
        return False
    system_prompt, user_prompt, retrieved_sources = i
    return o == [system_prompt, user_prompt] + retrieved_sources
