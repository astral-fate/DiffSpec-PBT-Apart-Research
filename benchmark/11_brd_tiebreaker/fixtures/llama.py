# Generated by DiffSpec-PBT live mode
# provider: nim
# model: meta/llama-3.3-70b-instruct
# timestamp_utc: 2026-05-25T00:04:41Z
# extract_status: ok
# elapsed_seconds: 17
def pre(i):
    return all(isinstance(x, tuple) and len(x) == 4 and all(isinstance(y, int) for y in x) for x in i)

def post(i, o):
    if not pre(i):
        return False
    sorted_i = sorted(i, key=lambda x: (-x[0], -x[1], -x[2], x[3]))
    return o == sorted_i
